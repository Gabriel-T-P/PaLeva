pt-BR:
  activerecord:
    attributes:
      portion_order:
        quantity: 'Quantity'
        observation: 'Observation'
        order: 'Order'
        portion: 'Portion'
    models:
      portion_order:
        one: 'Portion Order'
        other: 'Portions Orders'
    errors:
      models:
        portion_order:
          attributes:
            observation:
              too_short: " allow %{count} caracters as the minimun"